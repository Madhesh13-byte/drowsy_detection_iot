driver drowsy message sending system
first need to install django
it will create some default
then clone the required main file(like views.py,.....) from the git 
then run from the terminal
and use the postman as the interface to send the alert message
if it shows"alert message sent successfully" then the code works properly

